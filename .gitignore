# Dependências Node (só se usar o back-end)
node_modules/

# Variáveis de ambiente (chave da API)
.env
.env.local
.env.production

# Fotos enviadas localmente pela área ADM (em produção ficam no Supabase)
uploads/

# Sistema operacional
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp
*.swo

# Pastas de build de deploy (Cloudflare Pages)
dist-site/
dist-prototipo/

# Estado local do wrangler (cache/config da CLI do Cloudflare)
.wrangler/
